apiVersion: v1
kind: Pod
metadata:
  name: filter-test-pod
  labels:
    app: filter-test
spec:
  schedulerName: balanced-scheduler
  containers:
    - name: nginx
      image: nginx:1.27-alpine
      resources:
        requests:
          cpu: "50m"
          memory: "64Mi"
