apiVersion: apps/v1
kind: Deployment
metadata:
  name: balanced-scheduler-normal
  labels:
    app: balanced-scheduler-normal
spec:
  replicas: 6
  selector:
    matchLabels:
      app: balanced-scheduler-normal
  template:
    metadata:
      labels:
        app: balanced-scheduler-normal
    spec:
      schedulerName: balanced-scheduler
      containers:
        - name: pause
          image: registry.k8s.io/pause:3.10
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
